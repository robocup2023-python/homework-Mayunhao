可变（mutable）：可变对象是指对象创建后，可以修改其内容，而不改变对象的标识。常见的可变对象包括列表（list）和字典（dictionary）。当你对可变对象进行操作时，它们的内容可以改变，但它们的标识不会变化。
不可变（immutable）：不可变对象是指对象创建后，不能修改其内容，如果需要改变对象的内容，必须创建一个新对象。常见的不可变对象包括整数（int）、浮点数（float）、字符串（str）和元组（tuple）。
下面的代码报错是因为元组是不可变对象，一旦创建后，其元素不能被修改，所以会引发错误。
